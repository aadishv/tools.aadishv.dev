---

---

<script>
  function getSubpath() {
    // location.pathname already excludes query/hash
    const raw = window.location.pathname || "/";
    // remove all leading/trailing slashes
    const trimmed = raw.replace(/^\/+|\/+$/g, "");
    // optionally remove a trailing index file like "index.html" or "index.php"
    const withoutIndex = trimmed
      .replace(/(?:^|\/)index\.(?:html?|php)$/i, "")
      .replace(/^\/+|\/+$/g, "");
    return withoutIndex;
  }
  console.log(
    await fetch(`https://heartbeat-pearl.vercel.app/ping/${getSubpath()}`).then(
      (r) => r.text(),
    ),
    "analytics sent!",
  );
</script>
<!-- 100% privacy-first analytics --><!-- sorry for the DNT folks :( guess now I'm big tech :sob: -->
<script
  is:inline
  data-collect-dnt="true"
  async
  src="https://scripts.simpleanalyticscdn.com/latest.js"></script>
<noscript
  ><img
    src="https://queue.simpleanalyticscdn.com/noscript.gif?collect-dnt=true"
    alt=""
    referrerpolicy="no-referrer-when-downgrade"
  /></noscript
>
